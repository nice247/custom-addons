<odoo>
    <record id="property_view_tree" model="ir.ui.view">
        <field name="name">property tree</field>
        <field name="model">property</field>
        <field name="arch" type="xml">
            <tree multi_edit="1" class="head_custom" decoration-danger="(is_late)">
                <field name="name"/>
                <field name="owner_id"/>
                <field name="date_availability"/>
                <field name="sale_price"/>
                <field name="garden"/>
                <field name="bedrooms"/>
                <field name="is_late" invisible="True"/>
                <field name="facades" optional="1"/>
                <field name="expected_price" optional="1"/>
                <field name="postcode" optional="1"/>
                <field name="active" optional="1"/>

            </tree>
        </field>
    </record>

    <record id="property_view_form" model="ir.ui.view">
        <field name="name">property form</field>
        <field name="model">property</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <header>
                        <button name="action_draft" type="object" class="btn-primary" string="Set Draft"
                        invisible="status in ('draft', 'closed')"/>
                        <button name="action_pending" type="object" class="btn-secondary" string="Set Pending"
                        invisible="status != 'draft'"/>
                        <button name="action_approved" type="object" class="btn-success" string="Set Approved"
                        invisible="status != 'pending'"/>
                        <field name="status" widget="statusbar"/>
                    </header>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" readonly="status == 'closed'"/>
                        </h1>
                    </div>
                    <group string="General Info: " readonly="status == 'closed'">
                        <field name="date_availability" readonly="status == 'closed'"/>
                        <field name="expected_sale_date" readonly="status == 'closed'"/>
                        <field name="is_late" invisible="1" readonly="status == 'closed'"/>
                        <field name="expected_price" readonly="status == 'closed'"/>
                        <field name="sale_price" readonly="status == 'closed'"/>
                        <field name="deft" readonly="status == 'closed'"/>
                        <field name="living_area" readonly="status == 'closed'"/>
                        <field name="facades" readonly="status == 'closed'"/>
                        <field name="postcode" readonly="status == 'closed'"/>
                        <field name="description" readonly="status == 'closed'"/>
                    </group>

                    <group string="Additional Info: ">
                        <field name="owner_id" readonly="status == 'closed'"/>
                        <field name="owner_phone" readonly="status == 'closed'"/>
                        <field name="garden" readonly="status == 'closed'"/>
                        <field name="garden_area" readonly="status == 'closed'"/>
                        <field name="garden_orientation" readonly="status == 'closed'"/>
                        <field name="bedrooms" readonly="status == 'closed'"/>
                        <field name="garage" readonly="status == 'closed'"/>
                    </group>
                    <notebook>
                        <page string="Bedrooms" readonly="status == 'closed'">
                            <field name="line_ids">
                                <tree editable="bottom">
                                    <field name="description"/>
                                    <field name="area"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="property_search" model="ir.ui.view">
        <field name="name">property search</field>
        <field name="model">property</field>
        <field name="arch" type="xml">
            <search>
                <field name="postcode"/>
                <field name="name"/>

                <filter name="is_garden" string="with garden" domain="[('garden', '=', True)]"/>
                <filter name="group_garden" string="garden" context="{'group_by':'garden'}"/>
            </search>
        </field>
    </record>

    <record id="property_action" model="ir.actions.act_window">
        <field name="name">property action</field>
        <field name="res_model">property</field>
    </record>
    <record id="property_action_closed" model="ir.actions.server">
        <field name="name">Closed</field>
        <field name="model_id" ref="model_property"/>
        <field name="binding_model_id" ref="model_property"/>
        <field name="state">code</field>
        <field name="code">
            action = records.action_closed()
        </field>
    </record>
    <record id="property_wizard_action" model="ir.actions.server">
        <field name="name">Change Status</field>
        <field name="model_id" ref="model_property"/>
        <field name="binding_model_id" ref="model_property"/>
        <field name="state">code</field>
        <field name="code">
            action = records.action_status_wizard()
        </field>
    </record>
    <record id="property_expected_date" model="ir.cron">
        <field name="name">Property: Expected Date</field>
        <field name="model_id" ref="model_property"/>
        <field name="state">code</field>
        <field name="code">model.check_expected_sale_date()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
    </record>
    <menuitem
            id="property_menu_item"
            name="Properties"
            parent="properties_menu"
            action="property_action"
    />
</odoo>